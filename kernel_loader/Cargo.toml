[package]
name = "kernel_loader"
version = "0.1.0"
edition = "2024"

[dependencies]
multiboot = { path = "../multiboot" }
kernel_shared = { path = "../kernel_shared" }

[lib]
crate-type = ["staticlib"]